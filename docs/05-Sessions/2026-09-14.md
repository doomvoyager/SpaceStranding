---
date: 2026-09-14
tags: [session]
---

# Session 2026-09-14

The last of Mac's six ideas from the 13th: **first and third person, on foot
and in the rover, on one key.** Proposed on the 13th, built today. The cab
question the proposal left for Mac - the blockout is 0.77 m deck to roof, too
low for a seated eye - was answered by culling the hull from the eye rather
than by finding room inside it.

## Changed in the editor (Mac)

- Nothing this session.

## Changed in code (Claude)

- **`toggle_view`** - `V` / D-pad up, in `project.godot`, with render layers
  2 "Suit" and 3 "Rover hull" named.
- **`astronaut.gd` / `astronaut.tscn`** - an `Eye` camera under `CamPivot` at
  1.80 m (the head bone rests at 1.70, the crown at 2.03, measured by the new
  `probe_eye_height`); `first_person` and `suit_layers` exports; the suit's
  meshes put on their layer in `_dress_suit()` and dropped from the eye's mask;
  `_pitch_by()` writing pitch to both cameras; the body facing the look in
  first person; `disembark()` taking a heading; `_show_view()` making a camera
  current only on a real change.
- **`rover.gd` / `rover.tscn`** - an `Eye` in the cab, rigid to the chassis,
  aimed by `_aim_eye()` from the same yaw and pitch the chase rig uses; every
  hull mesh on layer 3 and the eye's mask without it; `first_person`;
  `view_heading()` handed to the astronaut on exit.
- **`stick_look.gd`** - `apply()` removed; both rigs read deltas now.
- **`hud.tscn`** - a `V / D-pad ↑ / First / third person` row on the card.
- **`test_view_toggle.tscn`** - the eye between head and crown, the layers,
  the aim unchanged by the view, pitch on both, the body's two behaviours,
  the rover's own remembered view, the eye rolling 40° while the chase pivot
  stops at 18, the handover facing 0.0° off, the panel gate, the F1 path.
- **`view_capture.tscn`** - the two views both ways, the inside of the helmet,
  the wedge the driver would stare at, the 30° roll from both cameras, and
  the shadow controls.

## Broke / noticed

- **The shadow control took four runs to get honest.** The first differenced
  two frames seconds apart with the idle animation running: 3.3/255, meaning
  nothing. Frozen and back to back, the regolith's dither alone reads
  1.7/255, so a whole-frame mean cannot see a shadow go; the instrument became
  "pixels that moved by more than 16/255" plus a difference image. Then the
  head lamp turned out to fill the figure's shadow in, so the controls run
  with it off and the sun raised to 35°.
- **The verdict on the layer:** moving the suit changed the chase frame by
  0.01% against a 0.01% floor; turning its `cast_shadow` off moved 0.07%,
  along the suit's self-shadow edges. Layers gate cameras only. Same as
  GrimdarkTank found.
- **Nothing casts a sun shadow onto the ground in `test_world`** - not the
  suit, the crates or a facility box, on the regolith or on a grey
  `StandardMaterial3D` swapped in for it - while the suit still shadows
  itself, and the crates trail dark shapes running the wrong way for the sun.
  The Sun is the only light with shadows on. Not the shader, not the layers.
  Unexplained, queued in [[The-Planet]]; it matters for "pure black shadows".
- The first capture kept the controls card in every frame: `find_child("HUD")
  as CanvasItem` is null for a `CanvasLayer`.
- macOS has no `timeout`; the Bash tool's own timeout does the job.

## Decided

Mac drove the rover: "feels nice, exactly what I had in mind with slower and
more careful driving." The lunar tuning stands.

Then the world. Asked for a procedural but believable lunar surface with
authored sections, and large. Proposed a composed heightfield - real LOLA base,
procedural detail below its resolution, authored stamps, tiles built around the
player with the horizon as the cutoff - and Mac chose: **the real south pole,
about 25 km across, the Gaea master retired.** In the [[Decision-Log]] and
[[Terrain]]. Two `#now` items opened: the sun-shadow probe and the real-ground
probe.

## The real ground, and the shadow

**The shadow probe** (`probe_sun_shadow.tscn`, twenty variants of a box under
the world's settings) found that sun shadows on the ground were fine all along
and that three artefacts had stacked up in the view capture - and, under them,
a real rule: **a camera's cull mask culls shadow casters from its own view**.
The suit hidden from the eye by layer had no shadow from the eye; the rover
hull likewise from the cab, which is what Mac saw. Hiding by
`SHADOWS_ONLY` instead keeps the shadow (0.20% of the eye's frame for the
suit, 29.6% of a cab frame pitched at the rover's own). Both rigs changed,
the "Suit" layer removed, layer 3 kept as the marker of what is exterior.

**The real ground** came down from NASA over HTTP through the new
`tools/lola-window.py` - 25.6 km at 20 m around the pole, then two 4.1 km
patches at 5 m. The pole is rugged: 66% of the wide window is at or under the
rover's 25°, 63% of the pole patch, 85% of the plateau patch, medians 17-22°.
The pole patch is the scene's ground now, origin on a 3° spot 1.3 km from the
pole with Shackleton's rim 600 m off. The macro reads; up close the 5 m data
is a smooth sheet, which is the detail layer's job. The Gaea bake stays in the
repo until nothing references it.

## Next

- **Playtest first person**, on foot and in the cab: eye positions, the 75°
  field, and whether the backpedal moonwalk matters.
- The detail layer below the DEM, then tiles built around the player.
- Still queued from the 13th: the horizon probe, the fiction half of the Moon,
  the purple regolith, sun movement, Earth in the sky, a reason for no GPS.

## Notes updated

- [[Astronaut-Traversal]] - "Views"; the item ticked.
- [[Rover]] - "The driver's eye".
- [[The-Planet]] - the ground-shadow finding.
- [[Decision-Log]] - the view key, per-context memory, the rigid cab eye, the
  culled hull.
- `CLAUDE.md` - the layers fact, the spring-arm-children fact, the
  CanvasLayer cast, the new test, capture and probe.
- `previews/2026-09-14/view-*` - twenty frames including the two difference
  images.
