---
date: 2026-09-02
tags: [session]
---

# Session 2026-09-02

Mac asked what was next, then set the order: make 0.55 g permanent, then build
the haulable mast. Both done. The mast took two commits because the survey and
the verb are separate ideas and the survey is the one that makes siting a
decision.

Also committed at the top of the session: the whole of 2026-09-01's second and
third halves, which had never been committed - the spawn gizmos, `SpawnPoint`,
the `TuningWriter` and [[Placement]].

## Changed in the editor (Mac)

- Nothing this session. One scene edit was asked for and granted: `Mast105` in
  `test_world.tscn` now carries `deploys_as`.

## Changed in code (Claude)

- **The planet is 0.55 g.** `surface_gravity` 3.34 -> **5.39 m/s^2**, mirrored
  into `project.godot`. Fifteen prose references to "0.34 g" followed it.
- **`max_engine_force` 900 -> 1170**, re-seated by bisection. See below.
- **`Lattice.survey_at(x, z)`** - the coverage question asked of ground that is
  not a site yet, answering with a new `SiteSurvey`. Scores every registered
  site and keeps the sturdiest, where sturdy is the *weakest* of two margins:
  link range left over, and the smallest gap between the sight line and the
  ground. Same rule `probe_relay_site` used to choose the world's own relay.
- **`Lattice.line_clearance()`**, kept separate from `has_line_of_sight()`
  because that one early-exits on the first blocked sample and this one has to
  see them all.
- **`Crate.deploys_as`** - what a crate becomes when raised. A property rather
  than a subclass, so a new deployable opts in by declaring what it becomes.
- **`Crate.raise_into()` and `Relay.lower()`** - the crate becomes the structure
  and is stowed inside it rather than consumed, so lowering hands back the same
  node with its damage intact.
- **`raise_mast` on `R` / gamepad `Y`**, with `KIND_MAST` wired into the
  astronaut's aim-scored target system and excluded from the other two verbs.
- **A carried survey readout in the HUD**, refreshed four times a second.
- New: `test_mast_survey`, `test_mast_readout`, `test_mast_raise`. Eighteen
  tests now, all green.

## Broke / noticed

- **0.55 g broke the rover, and no test noticed.** All fifteen regression tests
  passed at the new gravity with the old engine force, while ten seconds of full
  throttle over broken ground fell from **4.7 m/s and 29 m to 1.9 m/s and 7 m** -
  a hauler that can no longer haul. Grip scales with weight so the corners
  improved, but rolling resistance and climbing out of undulations scale with
  weight too, and on broken ground they win. Only `probe_carrier_jolt` saw it,
  because it is the only thing in the project that drives. Re-seated to 1170 by
  bisection against the old figures: same 29 m, load still pristine. 1450 covers
  37 m and starts scuffing cargo on an ordinary drive.
- **Falls now cost about twice as much.** `jolt_floor` and `jolt_ruin` are
  absolute m/s^2 and were deliberately left alone - a heavier planet making a
  drop more expensive is the physics working. A 7 m drop of the loaded rover
  went from costing a tenth of the load's condition to costing 0.22.
- **Every raised mast was going to be called "relay".** The code named one only
  if its id was blank, and `relay.tscn` carries an authored id - so the check
  never fired, and the second mast raised would have silently replaced the first
  in `_sites`, leaving a mast standing in the world that coverage could not see.
  It shipped green. What caught it was raising a *second* mast, which the test
  did not do; it does now. One is never enough to catch a name collision.
- **`is_stowed()` only knew about racks.** A crate riding inside a raised mast
  reported itself loose and went on sampling its own jolt from inside something
  static. Harmless today - it reads one gravity, well under the damage floor -
  and wrong in a way that would have surfaced later as mystery cargo damage.
- **Two commits appeared mid-session that Claude did not make** (`e74f538`,
  `ff5b9c7`), 22 seconds apart, sweeping up staged content and splitting it
  along the placement/tuning line with messages nobody here wrote. No git hooks
  in the repo. Content verified correct and complete against what was staged,
  and no gravity work leaked in. Flagged to Mac; unexplained.
- The four-times-a-second survey interval is a guess that has never been felt
  with a pad in hand.

---

# Session 2026-09-02 (second half)

Mac asked for relay range shown on the ground, like Death Stranding's chiral
network borders. Built - after arguing the shape first, because the obvious
reading of the request was the wrong one.

## Changed in code (Claude)

- **`CoverageMap`** - an R8 mask spanning the terrain patch, range intersected
  with line of sight, rebuilt on `Lattice.coverage_changed` and coalesced the
  same way the graph is. Each site rasterises only its own reach, so the cost
  is per-site and not per-map.
- **`Lattice.anchor_id`, `covering_sites()`, `coverage_at()`, `is_covered()`
  and a public `terrain()`.** The mask is the authority for the gameplay query
  as well as the picture.
- **`coverage_*` uniforms in `painterly.gdshader`**, sampled through the UV2
  the macro albedo already uses. Edge owns both channels; fill only tints
  `ALBEDO`; the scan pulse draws after so a ping wins.
- `CoverageMap` added to `debug_panel.gd`'s `_discover()`, and a `CoverageMap`
  node added to `test_world.tscn`.
- New: `test_coverage_map`, `coverage_capture`. Nineteen tests, all green.

## Broke / noticed

- **A ring would have contradicted the system it was drawing.** The request
  reads as circles; [[The-Lattice]]'s founding claim is that a radius makes the
  network a question of distance, which the map cannot argue with. Built as
  range *and* sight line instead, and agreed with Mac before writing it.
  Measured: a site sitting on the ground covers **82%** of the disc it sits in.
- **The spawn playa makes it look like a radius anyway.** In `test_world` the
  boundary around the Hearth is very nearly circular - because that ground is
  deliberately the flattest on the map. The mechanism is right and the location
  hides it. Worth knowing before judging the feature from the spawn shot.
- **The near-ground rectangles are confirmed pre-existing.** They show
  identically with the overlay off, which is what the paired capture is for.
  Not this work, and still unexplained since 2026-09-01.
- **`--headless` never builds a shader**, so the whole overlay could have
  failed to compile with all nineteen tests passing. `coverage_capture` runs
  windowed for exactly that reason.

---

# Session 2026-09-02 (third half)

Mac asked for a 3D minimap, opened as a panel, to plan a trip on. Three
branches were put up before building; Mac took all three recommendations, one
of them against a pillar and knowingly.

## Changed in code (Claude)

- **[[The-Map]]** - new note, new system. `MapPanel` on `M`, a SubViewport
  relief map with its own mesh and shader, markers, a route line and a leg
  list.
- **`MapTerrain`** - a low-resolution mesh from the same heightfield the game
  drives on, drawn unshaded with an elevation ramp, contours every 20 real
  metres and a hillshade from a conventional map light. Relief exaggerated
  2.5x; the ramp auto-ranges to the terrain and is gamma-curved.
- **`Route`** - a new autoload. Waypoints store X/Z and solve their height;
  legs are ground distances, not straight lines.
- **`Rover` lets go of its controls behind a panel**, and
  `Astronaut.set_menu_open()` is public so a panel on its own key can take the
  screen without going through a verb.
- New: `test_map_route`, `map_capture`. Twenty tests, all green.

## Broke / noticed

- **The map mesh's winding was inverted, and it did not look like a winding
  bug.** Wrong winding does not draw nothing: it draws the faces pointing
  *away*, so a heightfield seen from above renders as a few bright slivers on
  the steepest far slopes and everything else as void. That reads as a colour
  or lighting problem, and it cost a long chase through the elevation ramp,
  auto-ranging and a gamma curve — all three of which were real improvements
  and none of which was the bug. What named it was rendering the mesh one flat
  colour and **measuring** the result: 0.1% of the viewport drawn. New verified
  engine fact, and the winding is now copied from `terrain.gd`.
- **The map drew no coverage at all.** It and `CoverageMap` both hang off
  `Lattice.coverage_changed` and both rebuild deferred, so whichever ran first
  won — and when the map won it read a mask that did not exist yet, set
  `coverage_enabled` false and never looked again. It listens to the mask's own
  `rebuilt` now. A dependency on a *thing* should not be expressed as a
  dependency on the event that also causes it.
- **Two of the four capture framings were looking at empty space.** The terrain
  node carries a 1.3 km offset, so a camera framed on the world origin spends
  most of the frame off the patch — which read as "the map is black" and sent
  the colour chase off in the first place.
- **Showing terrain outside coverage contradicts a pillar.** Raised before
  building, Mac chose it, recorded in [[Decision-Log]] rather than left as a
  silent contradiction.
- Markers pile into unreadable noise when zoomed out. Open in [[The-Map]].

---

# Session 2026-09-02 (fourth half)

Mac specified both readings of a planned route in the world, and the arrival
rule with them: a beam on the next stop, the whole line on a scan pulse.

## Changed in code (Claude)

- **`RouteMarks`** - the light pillar on the nearest remaining stop, the route
  line revealed by a pulse, and the pointer triangle at your feet.
- **`route_beacon.gdshader`** - additive, fixed-Y billboarded, tapered top.
- **`Route` gained arrival**: `arrival_radius`, `nearest_index()`,
  `check_arrival()` and an `arrived` signal. Reaching a stop clears everything
  before it. Watched by `Route` itself, not by the marker node.
- **`Astronaut.vantage()`** - where the player actually is. The scanner's
  private copy was replaced by it; the HUD, the map panel and arrival now use
  it too.
- New: `test_route_marks`, `route_marks_capture`. Twenty-one tests, all green.

## Broke / noticed

- **A bug shipped earlier the same day.** The HUD's route bearing measured from
  `_astronaut.global_position`, which stops moving the moment you board a
  rover - so while driving it pointed from wherever you last parked. The
  scanner had had a private helper for exactly this since it was written, which
  is the tell: a rule with one copy in the codebase is a rule about to get a
  second, worse one. Now one `vantage()`, and a verified engine fact.
- **The test staged on physics frames and read nothing.** Both the beam and
  arrival are driven from `_process`, which does not interleave with
  `_physics_process` under `--headless` - the fact is already in CLAUDE.md and
  it still caught me. Worse, the stages re-ran their own setup every frame,
  clearing and re-adding the route faster than `Route` could tick a stop off
  it. Restaged to set up once and poll the state with a budget.
- **Additive over bright ground comes out white, for the third time.** The scan
  dot learned it, the coverage edge learned it, and the route line learned it
  again. Line and pointer mix now.
- The beam ended in a flat cut until it was tapered - `top_fade` alone leaves
  whatever it left at the quad's edge, which reads as a bar rather than light.

## Next

- **What masts cost.** Nothing consumes one and nothing pays for one; where
  masts come from is the entire economy of extending the network, and it is
  currently "the one in order 105". `#question` in [[The-Lattice]].
- **Order 105 and the verb disagree.** The order says bring the mast in;
  you can now stand it up in the field instead. Either the order becomes "raise
  it somewhere useful" or raising order-owned cargo is refused. Mac's call.
- **The readout answers "does this link", not "what does this buy".** No
  coverage preview from a prospective site. The `link_range` gizmo ring open in
  [[Placement]] is the editor half of the same gap.
- **Brakes are still un-instrumented** at 26 across both planets, and suspension
  stiffness now carries 62% more weight than it was set for. Both `#playtest`
  in [[Rover]].
- Nobody has looked at a raised mast. The relay scene is unchanged so it should
  read exactly like the authored one, but that is reasoning, not looking.

## Notes updated

- [[The-Lattice]] - "Surveying a site" and "Raising one" both new; the `#now`
  closed and replaced with three open questions the verb raised. `verified`
  bumped, still `partial`.
- [[Cargo]] - "Cargo you site, rather than deliver". `verified` bumped.
- [[Astronaut-Traversal]] - a third verb in reach. `verified` bumped.
- [[The-Planet]] - 0.55 g, and the bulk density that now implies. Radius and
  gravity together put Vesper c near Mercury at 5.8 g/cm^3, where 0.34 g had
  implied a suspiciously light 3.6.
- [[Rover]] - "Drivetrain, and what 0.55 g cost it", with the bisection table.
  Engine force left the `#playtest` list; brakes joined it.
- [[Decision-Log]] - the gravity change, and the three calls behind the raise
  verb (reversible, warned not refused, its own key) plus the rejected
  name-only-when-blank rule.
- `CLAUDE.md` - three new test commands.
