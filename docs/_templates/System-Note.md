---
status: design
verified: 2026-08-06
godot:
tags: [system]
---

# 

## Behaviour

What it does, in a few lines.

## Interactions

Which other systems this touches. Use [[wiki links]].

## Known issues

- [ ] 

## Open

- [ ] TODO: 

<!--
status:   built | partial | parked | inert | design | reference

          built     runs in the game a player walks through today
          partial   genuinely half-finished - some of it works, some is not
                    written yet. This is the honest backlog.
          parked    built and tested, and there is NO ROUTE to it. Reachable
                    only by opening a scene in the editor.
          inert     built and tested, and nothing CALLS it. Reachable, correct,
                    and no other system asks it anything.
          design    a note and no code
          reference not a system - a record, a workflow, a table

          `design` may carry a parenthetical naming what it was written
          against, when that thing is gone: `design (pre-V3)`. The intent is
          still wanted; the mechanism is not. Better than archiving a note
          whose reasoning is still correct, and honest in the build table in a
          way plain `design` is not.

          `parked` and `inert` were added 2026-08-27, when the docs audit found
          17 of 33 notes reading `partial` across five unrelated conditions.
          The build-status table stops being readable at that point. Both new
          words describe FINISHED work with a missing connection, which is a
          different job from half-built - and a much cheaper one to close.

godot:    res:// path to the script that implements this. If you cannot fill
          it in, the status is `design`, not `built`. That field is the drift
          detector - leaving it as a guess defeats the point. A `parked` or
          `inert` note DOES carry one; that is what makes it not `design`.

Task tags, aggregated by 00-Index:
  #now       at most three across the whole vault
  #next      queued
  #question  an open design decision
  #blocking  gates other work - use with #question
  #playtest  only a human on the controls will settle it - use with #question
Untagged tasks fall into the backlog, which is fine.
-->
